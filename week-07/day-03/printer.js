'use strict';
// - Create a function called `printer`
//   which logs to the console the input parameters
//   (can have multiple number of arguments)


let kecske = 'kecske';
let malac = 'malac'
let pocok = 'pocok'

let printer = function(a,b,c) {
    console.log(a + ' ' + b + ' ' + c);
}

printer(kecske,malac,pocok);